{
  "name": "hackunion-v2",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "prepare": "husky",
    "blogs:validate": "node scripts/validate-blogs.mjs",
    "blogs:generate": "node scripts/generate-blogs.mjs",
    "styles:compile": "mkdir -p styles public/styles && sass src/styles/main.scss styles/site.css --no-source-map --style=expanded && cp styles/site.css public/styles/site.css",
    "styles:watch": "mkdir -p styles && sass src/styles/main.scss styles/site.css --no-source-map --style=expanded --watch --poll",
    "predev": "npm run blogs:validate && npm run blogs:generate && npm run styles:compile",
    "prebuild": "npm run blogs:validate && npm run blogs:generate && npm run styles:compile",
    "dev": "concurrently -k \"npm:styles:watch\" \"vite\"",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "npm run build",
    "precommit": "npm run blogs:validate"
  },
  "devDependencies": {
    "concurrently": "^10.0.3",
    "husky": "^9.1.7",
    "sass": "^1.80.7",
    "vite": "^5.4.10"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "gsap": "^3.12.5",
    "jspdf": "^4.2.1",
    "lenis": "^1.1.14",
    "lucide": "^0.468.0",
    "markdown-it": "^14.1.0",
    "xlsx": "^0.18.5"
  }
}
